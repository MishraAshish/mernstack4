//Using back tick we can write our expressions (${}) withing the string instead of additionally evaluating them

let a = 5, b = 10;

console.log("Fifteen is "+ (a+b) 
    +"\n This is legacy new line") //legacy


console.log(`Print Fifteen ${a+b} using 
            Template Literal
            This is a smartwork 
            from ES6`)
